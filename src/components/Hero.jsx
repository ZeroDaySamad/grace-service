import React from 'react';

const Hero = () => {
    return (
        <section className="hero">
            <h1>Welcome to WhatsPlace</h1>
            <p>Your one-stop destination for all things premium.</p>
            <button className="cta-btn">Get Started</button>
        </section>
    );
};

export default Hero;
