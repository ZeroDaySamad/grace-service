import React from 'react';

const Header = () => {
    return (
        <header className="header">
            <div className="logo">WhatsPlace</div>
            <nav>
                <a href="#home">Home</a>
                <a href="#features">Features</a>
                <a href="#about">About</a>
            </nav>
        </header>
    );
};

export default Header;
